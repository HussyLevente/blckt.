# CARGO/00

Static frontend prototype for an underground sneaker and streetwear drop store.

Open `index.html` directly in a browser. All cart, saved items, newsletter, auth, checkout, and inventory behavior is mocked with `localStorage`; no backend or build step is required.
